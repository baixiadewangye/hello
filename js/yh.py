import pygame  import random # 初始化 pygame  pygame.init() # 设置屏幕大小  screen_width = 800  screen_height = 600  screen = pygame.display.set_mode((screen_width, screen_height)) # 设置烟花颜色列表  colors = [(255, 0, 0), (0, 255, 0), (0, 0, 255), (255, 255, 0), (255, 0, 255), (0, 255, 255)] # 设置烟花粒子速度  speed = 2 # 烟花粒子类  class FireworkParticle:     def __init__(self, x, y, color):         self.x = x         self.y = y         self.color = color         self.speed_y = speed    # 更新烟花粒子位置     def update(self):         self.y += self.speed_y    # 绘制烟花粒子     def draw(self, screen):         pygame.draw.circle(screen, self.color, (self.x, self.y), 3) # 创建烟花粒子列表  firework_particles = [] # 主循环  running = True  while running:     screen.fill((0, 0, 0))    # 添加新的烟花粒子     if random.random() < 0.02:         x = random.randint(0, screen_width)         y = 0         color = random.choice(colors)         firework_particles.append(FireworkParticle(x, y, color))    # 更新烟花粒子     for particle in firework_particles:         particle.update()         particle.draw(screen)        # 从屏幕中删除已经飞出屏幕的烟花粒子         if particle.y > screen_height:             firework_particles.remove(particle)    # 处理事件     for event in pygame.event.get():         if event.type == pygame.QUIT:             running = False    pygame.display.flip()     pygame.time.delay(20) # 退出 pygame  pygame.quit()  